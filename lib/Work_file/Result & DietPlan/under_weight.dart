import 'package:flutter/material.dart';

class UnderweightPage extends StatelessWidget {
  const UnderweightPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Underweight Page')),
      body: const Center(child: Text('This is the Underweight Page')),
    );
  }
}
