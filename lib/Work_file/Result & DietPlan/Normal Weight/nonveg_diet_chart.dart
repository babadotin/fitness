import 'package:flutter/material.dart';

class NonvegDietChart extends StatelessWidget {
  const NonvegDietChart({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nonveg Diet Plan Page')),
      body: const Center(
        child: Text('This is the Nonveg Diet Plan Page'),
      ),
    );
  }
}
