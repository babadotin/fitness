import 'package:flutter/material.dart';

class OverweightPage extends StatelessWidget {
  const OverweightPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Overweight Page')),
      body: const Center(child: Text('This is the Overweight Page')),
    );
  }
}
